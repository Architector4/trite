[workspace]
resolver = "3"

members = [
	"scratchpad",
	"bevy_mod_time_travel",
	"ensure_no_std",
]

# optimize dependencies
[profile.dev.package."*"]
opt-level = 3

# hehe performance go brr
[profile.release]
lto = "fat"
codegen-units = 1
